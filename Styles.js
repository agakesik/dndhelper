import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#3C8A90',
    accent: '#FFD343',
    disabled: '#245457',
    surface: '#245457',
  },
}